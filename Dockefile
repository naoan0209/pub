# 実行ステージ
FROM public.ecr.aws/amazoncorretto/amazoncorretto:21.0.2-al2023-headless
WORKDIR /app

# ビルドステージからコンパイル済みのMain.classをコピー
COPY sdktest-jar-with-dependencies.jar .

# アプリケーションの実行
CMD ["java", "-jar", "sdktest-jar-with-dependencies.jar"]
